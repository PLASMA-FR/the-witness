# Screenshot index

Usage screenshots for The Witness hackathon writeup and gallery.

### 01_project_folder.png

Shows: Shows the project folder, repository root, and main files.

Where to use it: Use at the start of the Usage section.

Suggested caption: “The demo starts from the real The Witness project folder.”

### 02_build_test.png

Shows: Shows `cargo build` and `cargo test` passing.

Where to use it: Use as proof the project builds and tests locally.

Suggested caption: “The Rust project compiles and the automated checks pass.”

### 03_doctor.png

Shows: Shows `the-witness doctor` readiness checks with pass/warn/fail statuses.

Where to use it: Use in setup/verification instructions.

Suggested caption: “Doctor reports what is ready and what still needs local setup.”

### 04_model_list.png

Shows: Shows configured Gemma/Ollama, Hugging Face, llama.cpp, LiteRT, and manual model options.

Where to use it: Use in the model setup section.

Suggested caption: “The Witness supports multiple Gemma deployment paths.”

### 05_tui_dashboard.png

Shows: Shows the main TUI dashboard with backend, endpoint, stats, and health state.

Where to use it: Use as the main product screenshot.

Suggested caption: “The Witness dashboard gives a live view of watched AI endpoints and verification status.”

### 06_model_manager.png

Shows: Shows the model manager and fine-tuned judge reference.

Where to use it: Use in technology-track and model sections.

Suggested caption: “The model manager connects Ollama, llama.cpp, LiteRT, Unsloth, and manual endpoints.”

### 07_endpoint_watchlist.png

Shows: Shows endpoint configuration with upstream URL, local proxy URL, profile, strictness, and retry limit.

Where to use it: Use in endpoint setup instructions.

Suggested caption: “Add an endpoint once; The Witness watches every request through the local proxy.”

### 08_live_request_stream.png

Shows: Shows request states: received, forwarded, judging, disapproved, retrying, approved.

Where to use it: Use when explaining live monitoring.

Suggested caption: “The live stream turns invisible AI traffic into an auditable timeline.”

### 09_verdict_prompt_repair.png

Shows: Shows a disapproved verdict, rejection reason, suggested fix, repaired prompt, and retry.

Where to use it: Use when explaining the core approval loop.

Suggested caption: “Bad responses are blocked, repaired, retried, and only released after approval.”

### 10_logs_audit.png

Shows: Shows the audit timeline and export command.

Where to use it: Use in audit/explainability section.

Suggested caption: “Every verification decision is saved as an audit trail.”

### 11_curl_test.png

Shows: Shows a localhost curl request and approved response through The Witness.

Where to use it: Use in the command-line usage section.

Suggested caption: “Applications call localhost; The Witness verifies before returning the response.”

### 12_huggingface_model.png

Shows: Shows the Hugging Face fine-tuned judge link and download command.

Where to use it: Use in Unsloth/fine-tuned model section.

Suggested caption: “The fine-tuned Witness judge is available as a Hugging Face adapter.”

### 13_colab_notebook.png

Shows: Shows the public Colab notebook link and fine-tuning workflow.

Where to use it: Use in reproducibility section.

Suggested caption: “The public Colab notebook makes the fine-tuning path reproducible.”
