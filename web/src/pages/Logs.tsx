import { CodeBlock } from '../components/CodeBlock';export function Logs({text,privacy}:{text:string;privacy:boolean}){return <><div className='topbar'><div><h1>Logs / Audit</h1><p style={{color:'var(--muted)'}}>Timeline, verdicts, repairs, retries, overrides, endpoint and judge errors.</p></div><div className='pill'>Privacy mode: {privacy?'on':'off'}</div></div><section className='card'><div className='row-actions'><button className='btn' onClick={()=>navigator.clipboard?.writeText(text)}>Copy JSONL</button><button className='btn'>Export Markdown</button><button className='btn'>Export CSV</button></div><CodeBlock value={text||'No audit events yet.'}/></section></>}
