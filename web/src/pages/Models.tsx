import type { ModelEntry } from '../types';import { ModelCard } from '../components/ModelCard';import { api } from '../api/client';
export function Models({models,links}:{models:ModelEntry[];links:Record<string,string>}){return <><div className='topbar'><div><h1>Model Manager</h1><p style={{color:'var(--muted)'}}>Gemma 4 E2B default, E4B high-risk, Unsloth/HF, llama.cpp, LiteRT, and manual judges.</p></div><div className='links'><a href={links.huggingface}>Hugging Face</a> · <a href={links.colab}>Colab</a></div></div><div className='grid' style={{gridTemplateColumns:'repeat(auto-fit,minmax(280px,1fr))'}}>{models.map(m=><ModelCard key={m.id} m={m} onTest={()=>api.modelTest({backend:m.backend,model:m.model})}/>)}</div></>}
