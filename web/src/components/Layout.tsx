import { Activity, Boxes, ClipboardList, FileText, HeartPulse, Home, RotateCw, Settings, Shield, TerminalSquare } from 'lucide-react';
const items = [['Dashboard',Home],['Endpoints',Boxes],['Requests',Activity],['Prompt Repair',RotateCw],['Human Review',HeartPulse],['Models',Shield],['Logs',FileText],['Doctor',ClipboardList],['Settings',Settings]] as const;
export function Layout({page,setPage,children}:{page:string;setPage:(p:string)=>void;children:React.ReactNode}){return <div className="app"><aside className="sidebar"><div className="brand"><TerminalSquare size={28}/> The <span>Witness</span></div><div className="pill" style={{marginTop:14}}>AI safety mission control</div><nav className="nav">{items.map(([name,Icon])=><button key={name} className={page===name?'active':''} onClick={()=>setPage(name)}><Icon size={16} style={{verticalAlign:'middle',marginRight:10}}/>{name}</button>)}</nav></aside><main className="main">{children}</main></div>}
